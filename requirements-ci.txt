# CI-only requirements — lean install, no native compilation
# pyscf is excluded (requires a C compiler and takes 5+ minutes to build).
# Module 02 smoke test (run_verification.py) uses a synthetic fallback when pyscf is absent.
# jupyter/notebook are excluded — not needed for smoke tests.

numpy>=1.26
scipy>=1.12
matplotlib>=3.8
scikit-learn>=1.4
networkx>=3.2
qiskit>=1.1
qiskit-aer>=0.14
qiskit-ibm-runtime>=0.23
qiskit-machine-learning>=0.7
qiskit-optimization>=0.6
qiskit-nature>=0.7
gymnasium>=0.29
torch>=2.0
